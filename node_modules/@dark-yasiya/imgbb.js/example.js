const { image2url } = require("./imgbb");
const path = require('path');

(async () => {
    const filePath = path.join(__dirname, './image/imgbb.jpg'); // Provide your image path
    const result = await image2url(filePath);

    console.log(result);
})();